import {ISettings} from "./interfaces/index";

export let Settings: ISettings = {
    canvasID: 'canvas-strike',
    canvasWidth: 1000,
    canvasHeight: 600,
    debugMode: false,
    gridSize: 20,
    fps: 40
};