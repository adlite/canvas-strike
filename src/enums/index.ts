export enum WallType {
    VERTICAL,
    HORIZONTAL
}

export enum Direction {
    LEFT,
    RIGHT,
    UP,
    DOWN
}

export enum Key {
    W = 87,
    A = 65,
    S = 83,
    D = 68,
    SPACE = 32,
    UP = 38,
    LEFT = 37,
    DOWN = 40,
    RIGHT = 39,
    SLASH = 191
}